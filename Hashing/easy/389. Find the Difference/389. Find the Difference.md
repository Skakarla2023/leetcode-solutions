## 389. Find the Difference

- You are given two strings s and t.
- String t is generated by random shuffling string s and then add one more letter at a random position.
- Return the letter that was added to t.


**Eg:**

```
Input: s = "abcd", t = "abcde"
Output: "e"
Explanation: 'e' is the letter that was added.
```


### Approach

- Use hashmap to store the character and its frequency.
- For every char in `t` compare its frequency in `s` and `t`.
- If any char doesn't exist in s or if any char occurs more times than s, then return it.


```java
 for (char ch : t.toCharArray()) {
            if (!map.containsKey(ch)) {
                return ch;
            }

            else if (map2.get(ch) > map.get(ch)) {
                return ch;
            }

        }
```
